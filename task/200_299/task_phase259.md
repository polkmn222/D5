Phase 259 Task Record

Scope
- Model-focused chat-native open/view bundle as the second safe step in the `brand/model` group.
- Keep scope limited to deterministic `Manage model <id>` / `OPEN_RECORD`, model chat-card response, selection/table `Open` chat-first routing, preserved-focus non-submit `OPEN_RECORD`, and model card `Open Record` display-first routing.

Approvals
- Continue automatically inside the grouped-object roadmap while the next step remained a safe continuation.
- Do not add model create/edit chat-native forms.
- Do not move into send/template implementation without checking for template/image complexity first.

Constraints
- Unit tests mandatory.
- DOM-level UI tests required for changed UI behavior.
- No manual testing.
