# Phase 231 Task

## Request

Normalize chat-native create and edit UX for approved AI-agent `OPEN_FORM` paths.

## Approved Scope

- Auto-scroll schema-based create/edit cards into view when rendered.
- Auto-scroll or preserve visibility when validation refresh replaces the form card.
- Route `contact` and `opportunity` selection/table Edit actions through chat-native `OPEN_FORM`.
- Keep workspace `OPEN_FORM` only as fallback for the approved phase objects.

## Constraints

- Keep scope narrow.
- Frontend-only if possible.
- Unit tests only.
- No manual testing.
- Do not change `OPEN_RECORD` post-submit behavior in this phase.
- Maintain `Implementation/`, `task/`, `Walkthrough/`, and `backups/` outputs.
