# Phase 73 Task

## Context

The repository needed one more pass on `.gitignore`, plus the latest user workflow directives had to be written into the canonical markdown guidance.

## Goals

- Tighten `.gitignore` for local-only files and duplicate cache locations.
- Store the new workflow directives in the canonical docs.
- Reaffirm that all active docs under `.gemini/development/docs/` must be consulted before implementation.
- Preserve phase-based task, implementation, walkthrough, and backup handling.
