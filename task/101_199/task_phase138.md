# Phase 138 Task

## Context

- The user requested that the local manual-test server logs be moved into the manual evidence tree.
- The user also requested that the markdown records explain where manual evidence is stored and how it should be stored.
- The `Activity` tab reference in the manual testing docs is no longer valid and should be removed.

## Goals

- Move the local manual-test server logs into a phase-scoped evidence folder under `test/manual/evidence/`.
- Update the manual testing docs to describe evidence storage locations and storage method.
- Remove outdated `Activity` tab references from the active manual checklist.
- Keep the phase documentation-only and do not modify runtime code.
