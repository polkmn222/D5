# Phase 183 Tasks

- [x] Analyze AI Agent Lead CRUD operation lifecycle.
- [x] Investigate and remove the double confirmation loop for deletions triggered via the chat UI.
- [x] Enforce frontend `AGENT_TABLE_SCHEMAS` in LLM-generated SQL queries (`display_name` and `model_name`).
- [x] Add `[FORCE_DELETE]` hidden flag handling in `ai_agent.js` and `service.py`.
- [x] Update agent prompt logic to strictly mandate `JOIN` requirements and field names.