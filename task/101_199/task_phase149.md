# Phase 149 Task

## Context

- The project already uses canonical docs under `/.gemini/development/docs/`, but the user asked for local folder guides that are easier to discover while working inside specific code areas.
- The recommended approach is to add short `README.md` files to the most important code folders instead of duplicating `skill.md` everywhere.

## Goals

- Add local `README.md` guides to `web/message`, `ai_agent`, `web/frontend`, `web/backend`, and `db`.
- Keep each local guide short, folder-specific, and clearly subordinate to the canonical docs.
- Preserve the new grouped-backup policy for phase artifacts.
