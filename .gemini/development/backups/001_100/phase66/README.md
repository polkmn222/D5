Active app code lives here.

- `backend/`, `frontend/`, `db/`, `ai_agent/` are the active runtime directories.
- `test/` holds active automated tests, manual test scripts, and test databases.
- `backups/vercel_render_legacy/` is an archived legacy deploy snapshot and is not used by current Vercel or Render deploys.
