# Phase 121 Task

## Context

AI Agent record actions should stay fully inside the chat surface. `Open` should render a detail view inside the AI Agent, `Edit` should render an in-chat editing panel, selection copy should prefer names over IDs, and template image upload needs clearer failure guidance plus loading feedback.

## Goals

- Render `Open` and `Edit` inside the AI Agent chat workspace.
- Use name-based selection copy where possible.
- Improve template image upload guidance with loading state and visible failure reasons.
