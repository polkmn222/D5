# Phase 116 Task

## Context

The AI Agent should reflect the updated recommendation labels and provide better guidance when users ask to send messages without selected records. Korean recent-record phrasing should also resolve like the English equivalents.

## Goals

- Apply the updated AI Recommend labels inside AI Agent flows.
- Replace the dead-end Send Message warning with guided recipient suggestions.
- Improve Korean recent-record query handling so phrases like `최근 생성 된 리드를 보여줘` map to the normal query path.
