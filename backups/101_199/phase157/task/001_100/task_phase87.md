# Phase 87 Task

## Context

- The Lead list page needed the top title/action UI moved down into the list-view card to match the requested layout.
- The previous gear control was also too limited and did not let users shape the list view themselves.

## Goals

- Move the Lead page title and top actions lower into the list-view card.
- Replace the gear action with a clearer `Setup` entry point.
- Let users customize the Lead list format in a practical first version.
- Keep the existing `All` / `Recently Viewed` behavior working.
